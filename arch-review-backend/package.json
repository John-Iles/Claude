{
  "name": "arch-review",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"API,UI\" --prefix-colors \"blue,green\" \"./venv/bin/uvicorn main:app --reload --port 8000\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "*"
  }
}
